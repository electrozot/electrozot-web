name: 🚀 Direct Deploy PHP Site via SFTP

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1 — Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2 — Direct SFTP upload
      - name: Deploy via SFTP
        uses: appleboy/scp-action@master
        with:
          host: 217.21.84.50
          port: 65002
          username: u848820288
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "./*"               # all files/folders in repo root
          target: "/public_html/"     # Hostinger web root
          overwrite: true
          strip_components: 0         # keep the folder structure as-is
          tar_exec: false
